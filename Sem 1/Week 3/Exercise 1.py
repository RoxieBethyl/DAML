import numpy as np
import matplotlib.pyplot as plt

# Generate random numbers
mean, sigma = 0, 0.1
s = np.random.normal(mean, sigma)

class ExponetialPDF:

    def __init__(self, mean, size):
        self.mean = 
